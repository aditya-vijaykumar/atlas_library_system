<div class="row mt-5">
  <div class="col-md-6 m-auto">
    <div class="card card-body text-center">
      <h1><i class="fab fa-ethereum fa-3x"></i></h1>
      <p>Create an account or login</p>
      <% if(!isAuthenticated){ %>
      <a href="/users/login" class="btn btn-primary btn-block">Login</a>
      <% } if(isAuthenticated){%>
      <a href="/dashboard" class="btn btn-primary btn-block">Enter Dashboard</a>
      <%  }%>
    </div>
  </div>
</div>
